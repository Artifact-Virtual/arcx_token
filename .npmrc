# Suppress platform-specific optional dependency warnings
fund=false
audit-level=moderate
optional=false

# Ignore fsevents on non-macOS platforms
target_platform=linux
target_arch=x64

# Fix peer dependency issues
legacy-peer-deps=true
